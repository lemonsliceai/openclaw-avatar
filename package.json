{
  "name": "@openclaw/video-chat",
  "version": "0.1.0",
  "description": "OpenClaw video chat plugin",
  "type": "module",
  "openclaw": {
    "extensions": [
      "./video-chat/index.ts"
    ]
  },
  "peerDependencies": {
    "openclaw": ">=2026.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0",
    "vitest": "^4.0.0"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  }
}
